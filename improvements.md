The next evolution would be:

“v3: autonomous scientist system”

Adds:

hypothesis generator (LLM-driven)
experiment prioritization policy (bandit/RL)
memory system (vector DB + experiment history)
automatic paper-style reporting
multi-agent competition (population-based training)

That is the point where this stops being a framework and becomes a research agent system.
